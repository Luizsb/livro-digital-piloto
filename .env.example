# Chave da API Gemini — usada pelo proxy local em npm run dev (não vai para o bundle).
# Obtenha em https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Legado (evitar em produção — expõe a chave no JavaScript compilado):
# VITE_GEMINI_API_KEY=
